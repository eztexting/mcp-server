{
  "name": "@eztexting/mcp-server",
  "version": "0.2.0",
  "description": "Official EZTexting MCP server bridge — connects local MCP clients to https://mcp.eztexting.com via Streamable HTTP + OAuth 2.1 PKCE.",
  "mcpName": "com.eztexting/mcp",
  "type": "module",
  "bin": {
    "eztexting-mcp": "dist/cli.js"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md",
    "server.json"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/eztexting/public/mcp-server.git"
  },
  "homepage": "https://mcp.eztexting.com",
  "license": "MIT",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "eztexting",
    "sms",
    "messaging"
  ],
  "dependencies": {
    "mcp-remote": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  }
}
